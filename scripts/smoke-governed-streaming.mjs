import { mkdtempSync } from 'node:fs'; import { tmpdir } from 'node:os'; import { join, resolve } from 'node:path';
import { loadAgentContracts } from '../packages/harness-runtime/lib/agent-contract.mjs'; import { evaluateAdmission } from '../packages/harness-runtime/lib/admission.mjs'; import { executeGovernedRole } from '../packages/harness-runtime/lib/execution.mjs';
const root=resolve('.'), project=mkdtempSync(join(tmpdir(),'ocode-stream-smoke-')); const {contracts}=loadAgentContracts({baseDir:root});
const admission=evaluateAdmission({contract:contracts.get('coder'),request:{schema_version:1,kind:'ASSIGNMENT',subject:{role:'coder'},requirements:{capabilities:[]},requested_authority:{edit:false,stage:false,commit:false,push:false}}});
const permission={edit:'deny',bash:'deny',read:'deny',glob:'deny',grep:'deny',list:'deny',skill:'deny',webfetch:'deny',websearch:'deny',task:'deny',question:'deny',external_directory:'deny'};
const result=await executeGovernedRole({baseDir:root,projectDir:project,role:'coder',profileName:'free',admissionDecision:admission,streaming:true,env:{...process.env,OPENCODE_DISABLE_EXTERNAL_SKILLS:'1',OPENCODE_DISABLE_CLAUDE_CODE_SKILLS:'1',OPENCODE_CONFIG_CONTENT:JSON.stringify({agent:{coder:{permission}}})},prompt:'Return exactly this JSON object and do nothing else: {"transport_smoke":"PASS"}'});
console.log(JSON.stringify({termination:result.execution.termination,exit_code:result.execution.exit_code,signal:result.execution.signal,duration_ms:result.execution.duration_ms,stdout_bytes:Buffer.byteLength(result.execution.stdout),stderr_bytes:Buffer.byteLength(result.execution.stderr),event_count:result.events.length,session_id:result.session_id,model_output_present:Boolean(result.model_output),binding:result.reconciliation.state,subject:result.subject_reconciliation.state})); console.log('GOVERNED_EXECUTION_RETURNED');
