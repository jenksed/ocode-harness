interface:
  display_name: "Ocode Harness"
  short_description: "Repo-specific patterns and workflows for ocode-harness"
  default_prompt: "Use the ocode-harness repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true