export function parseSkillResult(modelOutput, validator){const raw=String(modelOutput);const start=raw.indexOf('{');if(start<0)throw new Error('STRUCTURED_OUTPUT_INVALID: no JSON object');let value;try{value=JSON.parse(raw.slice(start));}catch{throw new Error('STRUCTURED_OUTPUT_INVALID: malformed JSON');}try{return validator(value);}catch(error){throw new Error(`STRUCTURED_OUTPUT_INVALID: ${error.message}`);}}
export function correctStructuredResult({initialOutput,validator,correct}){try{return {success:true,value:validator(initialOutput),repair_count:0};}catch(first){let second;try{second=correct(first.message);}catch(error){return {success:false,failure_classification:'STRUCTURED_OUTPUT_INVALID',structured_output_error:error.message,repair_count:1};}try{return {success:true,value:validator(second),repair_count:1};}catch(error){return {success:false,failure_classification:'STRUCTURED_OUTPUT_INVALID',structured_output_error:error.message,repair_count:1};}}}
