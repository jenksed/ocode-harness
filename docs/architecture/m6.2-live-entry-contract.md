# M6.2B live entry contract

M6.2B runs exactly one live TDD qualification through `qualify:tdd:live`. It must use the exact canonical fingerprint, a drift-free isolated projection, M4 Coder admission, an ephemeral load-only TDD permission, and a completed `tdd` skill event. Runtime evidence—not model claims—must establish RED/change/GREEN/scope before any qualified record exists.

M7 owns Control Ladder selection (L0 structural, L1 deterministic static, L2 executable, L3 qualified SkillProtocol, L4 human). Applicability is not selection. A bounded ProjectConstitution may replace project memory. M8 owns execution of selected controls; M10 may consider Control Graduation.
