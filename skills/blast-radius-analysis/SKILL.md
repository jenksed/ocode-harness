# Blast-Radius Analysis

Use only the supplied bounded repository context. Bound the proposed change, trace direct dependents, and trace indirect dependencies only with recorded justification. Consider contracts and verification surfaces. Separate direct impact, indirect impact, evidence, inference, uncertainty, and gaps. Do not mutate source, execute commands, design the solution, or broaden into an architecture survey. Return ReportedEvidenceCapsule v1 and stop when the bounded impact graph is sufficient.
